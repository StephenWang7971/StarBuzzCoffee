package com.sec.starbuzz.controller

import com.sec.starbuzz.model.Order

class Buy {
    public void buy(Order order) {
        // println drinkable.getName() + "(" + String.valueOf(drinkable.getPrice()) +")" + "|Total" + countTotal(drinkable, dressables);
    }

}
